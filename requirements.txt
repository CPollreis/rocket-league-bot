# Pinned to the versions currently in .venv (Python 3.12).
#
# torch: the CUDA 12.6 build is pulled from the PyTorch download channel via the
# --extra-index-url line below. The repo Dockerfile strips the torch== line and
# uses the torch from its PyTorch base image instead (see the Docker section in
# the README).

--extra-index-url https://download.pytorch.org/whl/cu126

numpy==1.26.4
torch==2.13.0+cu126
pydantic==2.13.5
wandb==0.29.0

# RLGym v2 + RocketSim + the rlgym-learn PPO stack
rlgym==2.0.1
rlgym-api==2.0.0
rlgym-rocket-league==2.0.1
rlgym-learn==2.0.0
rlgym-learn-algos==0.4.0
rlviser-py==0.6.13
rocketsim==2.2.1
